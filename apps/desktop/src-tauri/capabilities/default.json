{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only core window access is granted; backend and dialogs use narrow Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

