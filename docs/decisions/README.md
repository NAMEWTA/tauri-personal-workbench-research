# 架构决策入口

架构决策的唯一正文由 Speculo 管理，位于 [`speculo/.speculo/specdev/adr/`](../../speculo/.speculo/specdev/adr/)。本目录不复制 ADR 内容，只保留导航文件；新增或修改决策必须先更新 Speculo 正文。

当前决策还包括统一任务/日历模型、用户定义档案 schema、无 V1 兼容的开发重置和 portable workspace 锁，详见 Speculo ADR `0009` 至 `0012`。

