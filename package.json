{
  "name": "personal-workbench",
  "version": "0.2.5",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": "10.33.0"
  },
  "scripts": {
    "dev": "pnpm build:sidecar && pnpm --dir apps/desktop tauri dev",
    "dev:web": "pnpm --dir apps/desktop dev",
    "generate": "pnpm generate:ts && pnpm generate:go",
    "generate:ts": "pnpm --dir apps/desktop generate:api",
    "generate:go": "node scripts/generate-go.mjs",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "build:windows": "pnpm build:sidecar && pnpm --dir apps/desktop tauri build --bundles nsis",
    "build:macos": "pnpm build:sidecar && pnpm --dir apps/desktop tauri build --bundles dmg",
    "artifacts": "pnpm build:windows && powershell -ExecutionPolicy Bypass -File scripts/package-portable.ps1 && pnpm sbom && node scripts/generate-hashes.mjs",
    "check": "pnpm check:contract && pnpm check:frontend && pnpm check:go && pnpm check:rust",
    "check:contract": "pnpm exec redocly lint api/openapi.yaml",
    "check:frontend": "pnpm --dir apps/desktop check",
    "check:go": "node scripts/go.mjs -C services/workbenchd test ./... && node scripts/go.mjs -C services/workbenchd vet ./...",
    "check:rust": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace",
    "test": "pnpm --dir apps/desktop test && node scripts/go.mjs -C services/workbenchd test ./... && cargo test --workspace",
    "test:smoke": "pnpm --dir apps/desktop test:e2e",
    "verify:versions": "node scripts/verify-versions.mjs",
    "sbom": "node scripts/generate-sbom.mjs"
  },
  "devDependencies": {
    "@redocly/cli": "2.49.0",
    "yaml": "2.8.1"
  }
}
